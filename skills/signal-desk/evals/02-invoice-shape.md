# Eval 02 — every item is an invoice

Fixture: Issue 01 `latest.json` (8 items).

## FAIL
Output is a summary, a TAM slide, or a chatbot wrapper. Any item lacks `sell.product` and `sell.price`.

## PASS
5–8 items. Each has a number (stat) and a sell the operator can invoice this week.
